# MySQL
DB_HOST=localhost
DB_PORT=3306
DB_NAME=leitor_db
DB_USER=root
DB_PASS=

# App
PORT=3000

# Google Cloud Credentials
# Path to the Google Cloud service account JSON file used for authentication.
# Do NOT commit your real service account JSON into source control. Set this
# to the absolute or relative path where you'll store the file locally.
# Example: C:\keys\my-project-sa.json  or ./config/service-account.json
GOOGLE_APPLICATION_CREDENTIALS=./config/service-account.json